#!/bin/bash
#
# User Data to install the AWS cli
#
set -e
set -u
set -o pipefail

PLATFORM=$(uname -m)

apt-get update && apt-get install -y unzip

curl "https://awscli.amazonaws.com/awscli-exe-linux-${PLATFORM}.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install
aws --version
